import { createStore } from "vuex";
import budgetList from "./modules/usersList";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    budgetList,
  },
});